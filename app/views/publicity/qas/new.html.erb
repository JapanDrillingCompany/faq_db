<!-- Navigation Menu including Bread Crumb Menu
================================================== -->
<%= render 'shared/breadcrumbs' %>

  <div class="content">
    <h1><%= t("action.new", :default => "New")%>  <%= t("activerecord.models.qa", :default => "FAQ") %></h1>
    <div class="inner">
        <%= render 'form' %>
    </div>
  </div>
</div>