<%= simple_form_for(@help, :validate => true) do |f| %>
  <% if @help.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@help.errors.count, "error") %> prohibited this publicity_help from being saved:</h2>

      <ul>
      <% @help.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-md-12">
    <%= f.input :title %>
    <%= f.input :content, :as => :text, :input_html => { :class => "form-control", :rows => 2 } %>
  <%= f.button :wrapped, :cancel => publicity_helps_path %>
  </div>
<% end %>
